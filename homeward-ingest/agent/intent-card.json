{
  "schema": "autobuilder.intent_card.v1",
  "prd_source": "/home/jsy/Documents/PRDs/build-queue/PRD-build-homeward-ingest-autobuilder-onboard.md",
  "intent_slug": "build-homeward-ingest-autobuilder-onboard",
  "root_motivation": "Five whys (from `PRD-homeward-ingest-backfill`'s own iter_log, 2026-09-13):",
  "user_persona": "UNSPECIFIED \u2014 no existing intent card to carry forward from; set by hand (intent-card-refresh.sh cannot source this from a PRD).",
  "unfakeable_metric": {
    "name": "acceptance_tests_passing_count",
    "lower_is_better": false,
    "harness_command": "scripts/run-metrics.sh",
    "target": null
  },
  "acceptance_criteria": [
    {
      "id": "AC1",
      "level": "MUST",
      "description": "Given `homeward-ingest/agent/proof-lanes.toml` after this change, When every path under `homeward-ingest/src/` and `homeward-ingest/tests/` is checked against it, Then each resolves to >= 1 lane (mirrors the proof-lanes.toml header's own stated contract).",
      "test": "tests/acceptance_ac1.rs"
    },
    {
      "id": "AC2",
      "level": "MUST",
      "description": "Given `homeward-ingest/scripts/run-metrics.sh` (or the workspace-level equivalent, whichever `homeward-schema` actually wires into its gate) after this change, When it is run against `homeward-ingest`, Then it produces `target/autobuilder/metrics.json` matching the documented schema.",
      "test": "tests/acceptance_ac2.rs"
    },
    {
      "id": "AC3",
      "level": "MUST",
      "description": "Given a fresh `extend-gate.sh /home/jsy/wintermute/homeward --head <HEAD> --force` run after this change, When its output is inspected, Then it no longer names `supply-audit`, `license-audit`, or `sbom` as `no-receipt` for homeward-ingest paths.",
      "test": "tests/acceptance_ac3.rs"
    },
    {
      "id": "AC4",
      "level": "SHOULD",
      "description": "Given `homeward-ingest-backfill` (parked behind this PRD via `Depends-on`) after this PRD ships, When its gate re-runs, Then it reaches `pass` or `delta-pass` without a manually widened baseline.",
      "test": "tests/acceptance_ac4.rs"
    }
  ],
  "scope": [],
  "non_goals": [],
  "hard_constraints": {
    "rust_edition": "2024",
    "target_kind": "cli",
    "deny_unsafe": true
  },
  "five_whys_trace": [
    {
      "why": 1,
      "q": "Card auto-generated by intent-card-refresh.sh \u2014 was a 5-Whys interview conducted for it?",
      "a": "No. This card was mechanically regenerated from the landed PRD's frontmatter and body by scripts/intent-card-refresh.sh; no five-whys interview ran. See prd_source for the human-authored PRD (/home/jsy/Documents/PRDs/build-queue/PRD-homeward-ingest-backfill.md) this ships from."
    }
  ],
  "created_at": "2026-09-12T00:00:00Z"
}
