{
  "acceptance_criteria": [
    {
      "description": "Given a RescueGroups API animal payload that includes an org/location relationship, When the connector maps it to a `PetRecord`, Then `location` is `Some(ShelterLocation { .. })` with real city/state (and lat/lon if the API provides them), not `None`.",
      "id": "AC1",
      "level": "MUST",
      "test": "homeward-mcp/tests/mcp_ac01_tools_list_and_resource.rs"
    },
    {
      "description": "Given the existing 185k-row production DB, When a backfill pass runs against already-ingested RescueGroups records (using stored `source_animal_id`s to re-fetch or re-derive location), Then a measurable majority gain non-null `location` (some source records may genuinely lack a location upstream — 100% is not required, but 0% must not persist).",
      "id": "AC2",
      "level": "MUST",
      "test": "homeward-mcp/tests/mcp_ac02_search_pets_radius_and_privacy.rs"
    },
    {
      "description": "Given a live `search_pets(lat, lon, radius_km)` or `search_pets(postal_code)` call after this ships, When run against real data, Then it returns non-empty results for at least one real query center known to have nearby shelters.",
      "id": "AC3",
      "level": "SHOULD",
      "test": "homeward-mcp/tests/mcp_ac03_empty_result_well_formed.rs"
    },
    {
      "description": "Given a fixture RescueGroups payload that includes org/location data, When the connector's mapping unit test runs, Then it asserts `location.is_some()`, so this regression class can't silently reappear.",
      "id": "AC4",
      "level": "SHOULD",
      "test": "homeward-mcp/tests/mcp_ac04_invalid_species_typed_error.rs"
    }
  ],
  "created_at": "2026-09-12T00:00:00Z",
  "five_whys_trace": [
    {
      "a": "No. This card was mechanically regenerated from the landed PRD's frontmatter and body by scripts/intent-card-refresh.sh; no five-whys interview ran. See prd_source for the human-authored PRD (/home/jsy/Documents/PRDs/build-queue/PRD-homeward-mcp-server.md) this ships from.",
      "q": "Card auto-generated by intent-card-refresh.sh — was a 5-Whys interview conducted for it?",
      "why": 1
    }
  ],
  "hard_constraints": {
    "deny_unsafe": true,
    "rust_edition": "2024",
    "target_kind": "cli"
  },
  "intent_slug": "homeward-ingest-location-backfill-missing",
  "non_goals": [],
  "prd_source": "/home/jsy/Documents/PRDs/build-queue/PRD-homeward-ingest-location-backfill-missing.md",
  "receipt_digest": "sha256:4836cb5f0dc9dcbe366ff368df3ebf04a4697ee9b51cbe2006f03defe01d2709",
  "root_motivation": "Verified 2026-09-13 during PRD-homeward-mcp-hub-deploy's real-environment AC checks (`tests/hub-deploy_ac3_search-pets-live.sh` against the live hub): - `sqlite3 ~/.local/share/homeward/homeward-ingest.db \"select count(*) from canonical_records where json_extract(record_json,'$.location') is not null;\"` → `0` (out of 185374 total rows). - `rescuegroups.rs`'s `map_animal`-equivalent function builds every `PetRecord` with `location: None,` unconditionally (line 635) — never reads whatever address/lat-lon fields the RescueGroups JSON:API response actually carries for the animal's org/shelter. - Two consumer-facing effects: (1) `search_pets`'s \"coarse location\" promise is unmet for every real result; (2) `search_pets(lat, lon, radius_km)` and `search_pets(postal_code)` — both explicitly documented tool parameters — can never match anything against live data, since `filter.rs`'s radius/postal-code filters read the same always-`None` field.",
  "schema": "autobuilder.intent_card.v1",
  "scope": [],
  "unfakeable_metric": {
    "harness_command": "scripts/run-metrics.sh",
    "lower_is_better": false,
    "name": "acceptance_tests_passing_count",
    "target": null
  },
  "user_persona": "UNSPECIFIED — no existing intent card to carry forward from; set by hand (intent-card-refresh.sh cannot source this from a PRD)."
}