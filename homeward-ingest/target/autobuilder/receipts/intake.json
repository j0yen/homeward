{
  "acceptance_criteria": [
    {
      "description": "Given a dev DB seeded with 100 of 500 fixture animals, When backfill runs against a fixture RG server, Then the DB holds all 500 with zero duplicate RG ids.",
      "id": "AC1",
      "level": "MUST",
      "test": "tests/acceptance_ac1.rs"
    },
    {
      "description": "Given a backfill interrupted at ~50%, When the command re-runs, Then it resumes from persisted progress and the final DB matches AC1's result exactly.",
      "id": "AC2",
      "level": "MUST",
      "test": "tests/acceptance_ac2.rs"
    },
    {
      "description": "Given the fixture RG server returns 429 on every third request, When backfill runs, Then it completes with all animals present and the log shows backoff events, not failures.",
      "id": "AC3",
      "level": "MUST",
      "test": "tests/acceptance_ac3.rs"
    },
    {
      "description": "Given fixture animals with photos, When backfill completes, Then each has an embedding entry and the enrollment audit reads id_map.json as a list (audit count == list length for enrolled ids).",
      "id": "AC4",
      "level": "MUST",
      "test": "tests/acceptance_ac4.rs"
    },
    {
      "description": "Given the fixture RG server, When backfill paginates, Then requests use page-based parameters and no request carries an `offset` param.",
      "id": "AC5",
      "level": "MUST",
      "test": "tests/acceptance_ac5.rs"
    },
    {
      "description": "Given a completed run, When the report prints, Then it shows per-source fetched/inserted/skipped/failed and DB-total vs RG-total lines.",
      "id": "AC6",
      "level": "SHOULD",
      "test": "tests/acceptance_ac6.rs"
    },
    {
      "description": "Given `--dry-run`, When invoked, Then coverage and page estimate print and the DB mtime is unchanged.",
      "id": "AC7",
      "level": "SHOULD",
      "test": "tests/acceptance_ac7.rs"
    },
    {
      "description": "Given an empty DB, When backfill runs, Then it completes without special-casing (empty input edge).",
      "id": "AC8",
      "level": "MAY",
      "test": "tests/acceptance_ac8.rs"
    }
  ],
  "created_at": "2026-09-12T00:00:00Z",
  "five_whys_trace": [
    {
      "a": "No. This card was mechanically regenerated from the landed PRD's frontmatter and body by scripts/intent-card-refresh.sh; no five-whys interview ran. See prd_source for the human-authored PRD (/home/jsy/Documents/PRDs/build-queue/PRD-homeward-ingest-backfill.md) this ships from.",
      "q": "Card auto-generated by intent-card-refresh.sh — was a 5-Whys interview conducted for it?",
      "why": 1
    }
  ],
  "hard_constraints": {
    "deny_unsafe": true,
    "rust_edition": "2024",
    "target_kind": "cli"
  },
  "intent_slug": "homeward-ingest-backfill",
  "non_goals": [],
  "prd_source": "/home/jsy/Documents/PRDs/build-queue/PRD-homeward-ingest-backfill.md",
  "receipt_digest": "sha256:004dc5b7cd5ab67a48d556b1325f7225602833e8560b6407fed4cbac93d5d491",
  "root_motivation": "An owner searching for a pet sheltered before 2026-06-13 (or in any page the forward cursor skipped) gets zero results even when the pet is listed on RescueGroups — homeward has no record of it. Evidence: homeward-ops standing gap \"RG API ~62k animals, DB ~9.7k (pre-2026-06-13 cursor not backfilled)\" (verified 2026-08-31); RG v5 confirmed live with dogs+cats search endpoints 2026-09-12.",
  "schema": "autobuilder.intent_card.v1",
  "scope": [],
  "unfakeable_metric": {
    "harness_command": "scripts/run-metrics.sh",
    "lower_is_better": false,
    "name": "acceptance_tests_passing_count",
    "target": null
  },
  "user_persona": "UNSPECIFIED — no existing intent card to carry forward from; set by hand (intent-card-refresh.sh cannot source this from a PRD)."
}