# homeward-schema — autobuilder program

Generated by autobuilder Stage 2. Read in full before every iteration.

## What you are doing

You are the inner-loop coder for a Rust lib project being built under autobuilder's iterate-and-prove discipline. Your contract is `agent/intent-card.json`. Your job is to propose one diff per iteration; the outer loop runs the gates and decides advance or revert.

## Files

| Path | Read | Write |
|---|---|---|
| `src/**` | ✓ | ✓ |
| `agent/intent-card.json` | ✓ | ✗ (write `intent_card_amendment_request.json` instead) |
| `agent/AUTOBUILDER_PROGRAM.md` (this file) | ✓ | ✗ |
| `agent/owner-map.json`, `agent/test-map.json`, `agent/proof-lanes.toml` | ✓ | ✗ |
| `tests/acceptance_*.rs`, `tests/proptest_invariants.rs` | ✓ | ✗ |
| `scripts/run-metrics.sh`, `scripts/audit.sh`, `scripts/risk-gate.sh` | ✓ | ✗ |
| `Cargo.toml` (full) | ✓ | only via deps-only iteration with `description` prefix `deps:` |
| `Cargo.lock` | ✓ | regenerated by cargo on deps iterations |

## Hard gates (all must pass each iteration)

1. `cargo check --workspace`
2. `cargo clippy --workspace -- -D warnings`
3. `cargo test --workspace --no-fail-fast`
4. `cargo deny check bans licenses sources`
5. BAD_RUST audit scan (see `~/.claude/skills/autobuilder/rules/bad-rust.md`)

## Intent card summary

- **Root motivation**: Provide one canonical Rust type vocabulary (PetRecord, LostReport, Provenance) that all homeward crates share so heterogeneous shelter data sources degrade cleanly instead of requiring fictional defaults.
- **Target**: lib, MSRV 1.85, edition 2024, deny_unsafe = true
- **Unfakeable metric**: acceptance_tests_passing_count (target: 7)
- **ACs**: AC1–AC7 (all MUST)

## Current status

iter-1: SCAFFOLD COMPLETE — all 7 ACs green, all gates pass.
